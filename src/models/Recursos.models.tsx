export interface Hours {
    data: HoursData
}

export interface HoursData {
    code: number,
    number_hours: number,
    date: string,
    code_task: number,
    code_project: number,
    code_employee: number
}

